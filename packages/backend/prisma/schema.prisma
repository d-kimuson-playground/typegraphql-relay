datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Task {
  id          Int      @id @default(autoincrement())
  title       String
  detail      String
  isCompleted Boolean  @default(false)
  createdAt   DateTime @default(now())
}
